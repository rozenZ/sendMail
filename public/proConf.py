#/usr/bin/env python
#coding:utf-8

import os

homePath = os.path.dirname(os.path.split(os.path.realpath(__file__))[0])

EXECLPATH = homePath + '/excel/'
LOGPATH = homePath + '/log/'
CONFPATH = homePath + '/conf/'
